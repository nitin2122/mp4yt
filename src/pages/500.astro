---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
---

<Layout
  title="Internal Server Error (500) — mp4yt"
  description="An unexpected error occurred on our server. Return to the homepage or try again later. mp4yt allows downloading video and audio streams seamlessly."
  canonicalUrl="https://mp4yt.com"
  noindex={true}
>
  <Navbar />
  <main id="main-content">
    <section class="error-section">
      <div class="page-container error-content">
        <span class="error-code">500</span>
        <span class="error-eyebrow">ERROR_INTERNAL_SERVER</span>
        <h1 class="display-xl error-title">
          SERVER HICCUP.
        </h1>
        <p class="body-md error-subtitle">
          An unexpected error occurred on our end. Our backend team has been notified, and we are working to get it fixed.
        </p>
        <div class="error-actions">
          <a href="/" class="btn-primary btn-primary-lg">
            BACK TO HOMEPAGE
          </a>
          <button onclick="window.location.reload();" class="btn-secondary" style="height: 48px; cursor: pointer;">
            RELOAD PAGE
          </button>
        </div>
      </div>
    </section>
  </main>
  <Footer />
</Layout>

<style>
  .error-section {
    position: relative;
    background-color: var(--color-canvas);
    border-bottom: var(--border);
    min-height: calc(100vh - 64px - 280px);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: var(--sp-section) var(--sp-lg);
    text-align: center;
  }

  .error-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--sp-md);
    max-width: 600px;
  }

  .error-code {
    font-family: var(--font-headline);
    font-size: 120px;
    font-weight: 800;
    line-height: 1;
    color: var(--color-canvas-soft-2);
    letter-spacing: -0.05em;
  }

  .error-eyebrow {
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-on-primary);
    background-color: var(--color-error);
    padding: 4px 12px;
    border: var(--border);
  }

  .error-title {
    margin: 0;
    color: var(--color-ink);
  }

  .error-subtitle {
    margin: 0;
    color: var(--color-body);
    line-height: 1.6;
  }

  .error-actions {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--sp-md);
    margin-top: var(--sp-lg);
    width: 100%;
  }

  @media (max-width: 600px) {
    .error-section {
      min-height: calc(100vh - 64px - 400px);
      padding: var(--sp-4xl) var(--sp-md);
    }
    .error-code { font-size: 80px; }
    .error-actions {
      flex-direction: column;
      gap: var(--sp-sm);
    }
    .error-actions a, .error-actions button {
      width: 100%;
    }
  }
</style>
